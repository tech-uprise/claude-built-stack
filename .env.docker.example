# Docker Environment Configuration
# Copy this to .env for Docker Compose usage

# Node.js Application
NODE_ENV=production
PORT=3000

# PostgreSQL Database
DB_HOST=db
DB_PORT=5432
DB_NAME=radiocalco_dev
DB_USER=postgres
DB_PASSWORD=postgres

# Note: For production deployments, use strong passwords and consider:
# - Using Docker secrets
# - Environment-specific .env files
# - Secret management services (AWS Secrets Manager, HashiCorp Vault, etc.)
